import React from 'react';
import { describe, it } from 'vitest';
import { PlaylistSearch } from '#components/PlaylistSearch';

describe('the PlaylistSearch component', () => {
  it.todo('should have an input field', () => {});

  it.todo('should have a button that performs the search', () => {});

  it.todo('should display any errors that result from the search', () => {});

  it.todo('should let the user start the search with the enter key', () => {});
});

const testPlaylistSearch = () => {};
